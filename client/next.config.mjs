/** @type {import('next').NextConfig} */
const nextConfig = {
    eslint: {
      // This option disables ESLint during builds
      ignoreDuringBuilds: true,
    },
  };
  
  export default nextConfig;
  